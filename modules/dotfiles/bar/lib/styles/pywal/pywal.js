import colors from '/Users/lalit/.cache/wal/colors-speedcrunch.json';

export const variables = {
  cursor: colors.cursor,
  number: colors.number,
  parens: colors.parens,
  result: colors.result,
  comment: colors.comment,
  matched: colors.matched,
  function: colors.function,
  operator: colors.operator,
  variable: colors.variable,
  scrollbar: colors.scrollbar,
  separator: colors.seperator,
  background: colors.background,
  editorbackground: colors.editorbackground,
};
